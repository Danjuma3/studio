{
  "entities": {
    "Ingredient": {
      "title": "Ingredient",
      "description": "User's private ingredient inventory with cost tracking.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "unitOfMeasure": { "type": "string" },
        "bulkUnitPrice": { "type": "number" },
        "retailUnitPrice": { "type": "number" },
        "weeklyUsage": { "type": "number" },
        "currentStock": { "type": "number" },
        "minStock": { "type": "number" }
      }
    },
    "Recipe": {
      "title": "Recipe",
      "description": "A collection of ingredients forming a salable dish.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "sellingPrice": { "type": "number" },
        "items": { "type": "array" }
      }
    },
    "SystemPaymentConfig": {
      "title": "SystemPaymentConfig",
      "description": "Global settings for branding and payments.",
      "type": "object",
      "properties": {
        "bankName": { "type": "string" },
        "accountNumber": { "type": "string" },
        "accountName": { "type": "string" },
        "paystackPublicKey": { "type": "string" },
        "proPrice": { "type": "number" },
        "proPriceUSD": { "type": "number" },
        "appLogoUrl": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/system/payment": {
      "schema": { "$ref": "#/backend/entities/SystemPaymentConfig" },
      "description": "Global branding and billing configuration. Publicly readable."
    },
    "/system/alert": {
      "schema": { "type": "object" },
      "description": "Global market news and system alerts. Publicly readable."
    },
    "/users/{userId}/ingredients/{ingredientId}": {
      "schema": { "$ref": "#/backend/entities/Ingredient" },
      "description": "Private ingredient list for each restaurant."
    },
    "/users/{userId}/recipes/{recipeId}": {
      "schema": { "$ref": "#/backend/entities/Recipe" },
      "description": "Private recipe costing for each restaurant."
    }
  }
}