
{
  "entities": {
    "Ingredient": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Ingredient",
      "type": "object",
      "description": "Represents a single raw food item or component used in recipes, storing its core pricing and measurement information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Ingredient entity."
        },
        "name": {
          "type": "string",
          "description": "The common name of the ingredient (e.g., 'Tomato', 'Flour')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description or additional notes about the ingredient."
        },
        "unitOfMeasure": {
          "type": "string",
          "description": "The standard unit of measure for this ingredient (e.g., 'kg', 'liter', 'piece', 'g')."
        },
        "bulkUnitPrice": {
          "type": "number",
          "description": "The price per unit of measure when the ingredient is purchased in bulk."
        },
        "retailUnitPrice": {
          "type": "number",
          "description": "The price per unit of measure when the ingredient is purchased at retail."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the ingredient record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the ingredient record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "unitOfMeasure",
        "bulkUnitPrice",
        "retailUnitPrice",
        "createdAt",
        "updatedAt"
      ]
    },
    "Recipe": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Recipe",
      "type": "object",
      "description": "Defines a complete recipe, including its name and a collection of ingredients with specified quantities.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Recipe entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the recipe (e.g., 'Jollof Rice', 'Efo Riro')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description or instructions for the recipe."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the recipe record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the recipe record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt",
        "updatedAt"
      ]
    },
    "RecipeIngredient": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RecipeIngredient",
      "type": "object",
      "description": "Links a specific ingredient to a specific recipe, including the quantity of that ingredient used in the recipe.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the RecipeIngredient entity."
        },
        "recipeId": {
          "type": "string",
          "description": "Reference to the Recipe this ingredient belongs to."
        },
        "ingredientId": {
          "type": "string",
          "description": "Reference to the Ingredient used in this recipe."
        },
        "quantity": {
          "type": "number",
          "description": "The amount of the ingredient used in the recipe."
        },
        "notes": {
          "type": "string",
          "description": "Any specific preparation notes."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when this record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when this record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "recipeId",
        "ingredientId",
        "quantity",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ],
    "customClaims": {
      "admin": {
        "type": "boolean",
        "description": "Grants global read/write access to all kitchen prof data."
      }
    }
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/ingredients/{ingredientId}",
        "definition": {
          "entityName": "Ingredient",
          "schema": {
            "$ref": "#/backend/entities/Ingredient"
          },
          "description": "User-specific ingredient data."
        }
      },
      {
        "path": "/users/{userId}/recipes/{recipeId}",
        "definition": {
          "entityName": "Recipe",
          "schema": {
            "$ref": "#/backend/entities/Recipe"
          },
          "description": "User-specific recipe data."
        }
      }
    ]
  }
}
